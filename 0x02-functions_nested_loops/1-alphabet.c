#include <main.h>
/**
 * main -entry point
 * Return: Always 0 (Success)
 */
char main(void)
{
		char alpha;

		for (alpha = 'a'; alpha <= 'z'; alpha++)
		{
		printf("%c\n", alpha);
		}
		{
		printf("\n");
		}
		return (0);
}
