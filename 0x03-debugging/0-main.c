#include "main.h"

/**
* main - Tests function to determine if integer is positive or negative
* Return: Always 0 (Success)
*/

int main(void)
{
int i;

i = 0;
positive_or_negative(i);

return (0);
}
